# Re-generating the mock
If the auth.Controller interface should change, the mock should also change.

It was generated by running command:
```console
mockgen . Controller | gofmt -s > mock/controller.go
```
in the parent directory.
